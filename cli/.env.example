# DLMM CLI 环境变量配置示例
# 复制此文件为 .env 并填入您的配置

# Solana 网络配置
# 可选值: mainnet-beta, testnet, devnet, localnet
CLUSTER=mainnet-beta

# 钱包配置
# 钱包密钥文件路径
WALLET_PATH=~/.config/solana/id.json

# RPC 节点配置（可选）
# 使用自定义 RPC 节点以提高性能
# RPC_URL=https://api.mainnet-beta.solana.com

# 交易配置
# 优先费用（micro lamports）- 用于加速交易确认
PRIORITY_FEE=10000

# 重试配置
# 最大重试次数
MAX_RETRIES=3
# 重试延迟（秒）
RETRY_DELAY=5

# 日志级别
# 可选值: error, warn, info, debug, trace
LOG_LEVEL=info

# ILM（初始流动性管理）配置
# 默认曲率参数
ILM_DEFAULT_CURVATURE=0.5
# 默认价格范围百分比
ILM_DEFAULT_RANGE_PERCENTAGE=50

# 开发环境配置
# 启用调试模式
DEBUG=false
# 跳过交易确认（仅用于测试）
SKIP_PREFLIGHT=false